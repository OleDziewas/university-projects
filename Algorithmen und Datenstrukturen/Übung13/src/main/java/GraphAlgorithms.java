import java.util.*;

/**
 * Implements graph algorithms for detecting Hamiltonian paths and cycles in a graph.
 * 
 * @author Michail Sioutis
 * @author Tobias Schwartz
 */
class GraphAlgorithms {

	/**
	 * Returns all the Hamiltonian paths in a graph.
	 * 
	 * @param adjMatrix a graph represented as an adjacency matrix
	 * @return an array of Strings, as specified in the assignment.
	 */
	public String[] identifyAllHamiltonianPaths(int[][] adjMatrix)  {
		return new String[]{};
	}

	/**
	 * Returns all the Hamiltonian cycles in a graph.
	 * 
	 * @param adjMatrix a graph represented as an adjacency matrix
	 * @return an array of Strings, as specified in the assignment.
	 */
	public String[] identifyAllHamiltonianCycles(int[][] adjMatrix)  {
		return new String[]{};
	}
}
